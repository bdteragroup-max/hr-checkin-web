import React from 'react';
import TrainingManager from '@/components/TrainingManager';

export const metadata = {
  title: 'ระบบจัดการการฝึกอบรม | HR Admin',
};

export default function TrainingsPage() {
  return <TrainingManager />;
}
