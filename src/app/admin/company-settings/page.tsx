import React from "react";
import ClientSettings from "./ClientSettings";

export const metadata = {
    title: "Company Settings | HR Check-in",
};

export default function CompanySettingsPage() {
    return <ClientSettings />;
}
